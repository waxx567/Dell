#include <iostream>

int main() {
    
    // type conversion = converting one data type to another
    //                   implicit = automatic
    //                   explicit = precede value with new data type

    return 0;
}