#include <iostream>

int main() {

    // break = used to exit a loop
    // continue = used to skip an iteration of a loop

    for (int i = 1; i <= 20; i++) {
        
        std::cout << i << std::endl;
    }

    return 0;
}