# Wherein the idea of a sorting hat is implemented
class Hat():
    def sort(self, name):
        print(name, "is in", "some house")


# Instantiate a Hat object and store it in the hat variable
hat = Hat()
# The sorting hat has a method (function) inside it called sort
hat.sort("Harry")